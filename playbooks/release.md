# Release

Verify the exact version, changelog, artifacts, checksums, dependency audit, full validation, and rollback instructions. Publishing, tagging, pushing, and deployment require explicit instructions.
